function updateLight(current) {
    // no variables needed
    // green, yellow, red, green
    //returns updated lights
    // turnary operators to update light color
    
  
    return current === 'yellow' ? 'red' : current === 'green' ? 'yellow' : 'green';
    
    
  
  }